node_modules
.cursor
# Every .env, including hand-made backups like .env.bak.1789567575 — the bare
# ".env" line used to match only that exact name.
.env*
!.env.example
client/.env*
dist
docs/whatsapp.env
# Vendor API notes pasted with live bearer tokens / session cookies in them
CUSTOMER CREATION API DETAILS.txt
# Vendor credentials pasted in by hand (Cashfree VRS client id/secret)
third-party-integrations/
